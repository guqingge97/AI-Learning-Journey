# Git 工作流程指南

## 🎯 目的
记录本项目的 Git 操作流程，确保提交规范和一致性

---

## 📋 项目结构
```
AI-Learning-Journey/
├── README.md
├── docs/                    # 核心文档（7+1个）
│   ├── 01-教学协议.md
│   ├── 02-学员档案.md
│   ├── 03-学习路线总览.md
│   ├── 04-学习进度追踪.md
│   ├── 05-教学风格记录.md
│   ├── 06-代码规范与模板.md
│   ├── 07-常见问题FAQ.md
│   └── 08-Git工作流程.md    # 本文件
├── notes/
│   └── Month-1.md           # 月度笔记
├── projects/                # 项目代码
└── progress.md              # 学习进度
```

---

## 🔄 日常 Git 操作流程

### 1️⃣ 每天学习完成后（更新笔记）

**场景：** 学完一天的内容，追加笔记到 Month-X.md
```bash
# 1. 确保在项目根目录
cd ~/AI-Learning-Journey

# 2. 查看修改状态
git status

# 3. 添加修改的文件
git add notes/Month-1.md

# 4. 提交（规范的 commit message）
git commit -m "Day X: 简短描述今天学的内容"

# 例如：
# git commit -m "Day 1: 列表推导式基础"
# git commit -m "Day 2: 列表推导式进阶"

# 5. 推送到 GitHub
git push
```

---

### 2️⃣ 每周完成项目后（上传代码）

**场景：** 完成一个学习项目，上传到 projects/
```bash
# 1. 添加项目文件夹
git add projects/week-1-text-processor/

# 2. 提交
git commit -m "Week 1 项目: 文本处理工具"

# 3. 推送
git push
```

---

### 3️⃣ 每2周更新文档（更新进度）

**场景：** Claude 提供了更新的文档，需要替换
```bash
# 1. 添加更新的文档
git add docs/04-学习进度追踪.md
git add docs/05-教学风格记录.md

# 2. 提交
git commit -m "Week X 更新: 学习进度和教学风格"

# 3. 推送
git push
```

---

### 4️⃣ 月度总结（整理归档）

**场景：** 一个月学习结束，整理总结
```bash
# 1. 更新 README
git add README.md

# 2. 更新 progress.md
git add progress.md

# 3. 提交
git commit -m "Month 1 完成: Python 工程基础"

# 4. 推送
git push
```

---

## 📝 Commit Message 规范

### 格式
```
类型: 简短描述（50字以内）
```

### 常用类型

- **Day X**: 每日学习笔记
- **Week X**: 每周项目
- **Month X**: 月度总结
- **docs**: 文档更新
- **fix**: 修复错误
- **chore**: 日常维护

### 示例
```bash
# 好的 commit message ✅
git commit -m "Day 5: 装饰器深入"
git commit -m "Week 2 项目: Python 工具库"
git commit -m "docs: 更新学习进度"
git commit -m "Month 1 完成: Python 工程基础"

# 不好的 commit message ❌
git commit -m "更新"
git commit -m "add files"
git commit -m "修改了一些东西"
```

---

## 🔧 常见 Git 命令

### 查看状态
```bash
git status              # 查看当前状态
git log                 # 查看提交历史
git log --oneline       # 简洁的提交历史
```

### 撤销操作
```bash
# 撤销未提交的修改
git checkout -- 文件名

# 撤销已 add 但未 commit 的文件
git reset HEAD 文件名

# 修改最后一次 commit message
git commit --amend -m "新的 commit message"
```

### 同步
```bash
# 拉取远程最新代码（如果多设备使用）
git pull

# 查看远程仓库
git remote -v
```

---

## 🚨 紧急情况处理

### 场景1：不小心提交了错误内容
```bash
# 1. 撤销最后一次 commit（保留修改）
git reset --soft HEAD^

# 2. 修改文件

# 3. 重新提交
git add .
git commit -m "正确的 commit message"
git push -f  # 强制推送（谨慎使用）
```

### 场景2：误删了重要文件
```bash
# 恢复到最近一次 commit 的状态
git checkout HEAD -- 文件名
```

### 场景3：想放弃所有本地修改
```bash
# ⚠️ 危险操作：放弃所有未提交的修改
git reset --hard HEAD
```

---

## 📅 定期维护

### 每周检查
```bash
# 查看当前分支
git branch

# 查看远程同步状态
git status
```

### 每月备份
- GitHub 已经是云备份
- 本地代码也是备份
- 双重保险

---

## ⚙️ Git 配置（首次使用）

如果是第一次使用 Git，需要配置：
```bash
# 配置用户名
git config --global user.name "你的名字"

# 配置邮箱
git config --global user.email "你的邮箱"

# 查看配置
git config --list
```

---

## 🔗 快速参考

### 最常用的 3 个命令
```bash
git add .                              # 添加所有修改
git commit -m "Day X: 描述"           # 提交
git push                               # 推送
```

### 完整流程（每天）
```bash
cd ~/AI-Learning-Journey
git add notes/Month-1.md
git commit -m "Day X: 今天学的内容"
git push
```

---

## 📌 注意事项

1. **commit 前先检查**：用 `git status` 确认要提交的文件
2. **commit message 要清晰**：让未来的自己能看懂
3. **每天至少 commit 一次**：养成习惯
4. **不要提交敏感信息**：API Key、密码等

---

## 更新日志
- 2026-01-05：创建 Git 工作流程文档